@model IEnumerable<TravelAgency.Models.Appointments>

@{
    ViewData["Title"] = "Index";
  
}

<p>
    <a asp-action="Create" class="btn btn-success"> <span class="fa fa-plus"> ميعاد جديد </span></a>
</p>
<table class="table table-striped table-bordered table-hover" id="table">
    <thead>
        <tr>
            <th>
                الاسم
            </th>
            <th>
                الوصف
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>

                <td>
                    <a asp-action="Edit" asp-route-id="@item.AppointmentId" class="btn btn-primary">  <span class="fa fa-edit"></span></a>
                    <a asp-action="Details" asp-route-id="@item.AppointmentId" class="btn btn-info">  <span class="fa fa-info"></span></a>
                    <a asp-action="Delete" asp-route-id="@item.AppointmentId" class="btn btn-danger"><span class="fa fa-trash"></span></a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts
        {

    <script type="text/javascript">

        $(document).ready(function () {
            $('#table').DataTable(

            );
        });
    </script>

}