@model TravelAgency.ViewModels.TicketViewModel

@{
  ViewData["Title"] = "حجز التذاكر";
  TravelAgency.ViewModels.ReservedTickets seat;
  var seatNumber = 0;
}
<form asp-action="CreateNotAdmin">

  <div class="row">
    <div class="form-group col-sm-4">
      <label asp-for="AppointmentId" class="control-label">الميعاد</label><br/>
      <select asp-for="AppointmentId" class="form-control" asp-items="Model.AppointmentsList"></select>
    </div>

    <div class="form-group col-sm-3">
      <label asp-for="TicketDate" class="control-label">يوم الحجز</label><br/>
      <input data-seat-num="@seatNumber" asp-for="TicketDate" class="form-control" />

      <span asp-validation-for="TicketDate" class="text-danger"></span>
    </div>
  </div>

  <div class="form-group row col-12">
    <input type="submit" value="عرض مفصل"
           name="okshow" asp-action="MyTickets" class="btn btn-info" />
  </div>
</form>

  <hr />
  <div class="row" style="color:red;font-weight:bold">
  
    <div class="col-sm-12 col-md-12  ">

    <div class="row pt-1">
      <div class=" col-sm-1  col-md-1"> </div>
      @{
        seatNumber = 50;
        seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

        if (  seat == null)
        {

          <div style="height: 130px" class="btn btn-success col-md-2">
            <label class="control-label">@seatNumber </label>
          </div>
        }
        else if (seat.IsMine)
        {
          <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
            <label class="control-label">@seatNumber - @seat.Supplier</label><br/>
            <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br/>
            <label class="control-label"> @(seat.Customer)</label>
            <br/>
            <label class="control-label">@seat.Phone</label>
          </div>
        }
        else
        {
          <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
            <label class="control-label">@seatNumber </label>
          </div>
        }

      }

    </div>
      <!--#region ----1 2 3 4-------  -->
      <div class="row pt-1">
        @{
          seatNumber = 1;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
            
          <label class="control-label">@seatNumber </label>
            </div>
          }

        }
        &nbsp;

        @{
          seatNumber = 2;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
             
               <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        <div class="col-md-1 col-sm-1"></div>
        @{
          seatNumber =3;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {
            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
                <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
        @{
          seatNumber = 4;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
          <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
      </div>
      <!--#endregion -->
      <!--#region ----5 6 7 8-------  -->
      <div class="row pt-1">
        @{
          seatNumber = 5;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
             <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        @{
          seatNumber = 6;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
             
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        <div class="col-md-1 col-sm-1"></div>
        @{
          seatNumber = 7;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
             <label class="control-label">@seatNumber </label>
              
            </div>
          }
        }
        &nbsp;
        @{
          seatNumber = 8;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
            <label class="control-label">@seatNumber </label>
              
            </div>
          }
        }
        &nbsp;
      </div>
      <!--#endregion -->
      <!--#region ----9 10 11 12-------  -->
      <div class="row pt-1">
        @{
          seatNumber = 9;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">

              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        @{
          seatNumber = 10;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        <div class="col-md-1 col-sm-1"></div>
        @{
          seatNumber = 11;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
  <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
        @{
          seatNumber = 12;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
      </div>
      <!--#endregion -->
      <!--#region ----13 14 15 16-------  -->
      <div class="row pt-1">
        @{
          seatNumber = 13;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        @{
          seatNumber = 14;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        <div class="col-md-1 col-sm-1"></div>
        @{
          seatNumber = 15;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
        @{
          seatNumber = 16;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
      </div>
      <!--#endregion -->
      <!--#region ----17 18 19 20-------  -->
      <div class="row pt-1">
           
            @{
          seatNumber = 17;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        @{
          seatNumber = 18;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
                        <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
            <div class=" col-md-1 col-sm-1 "></div>
            @{
                seatNumber = 19;
                seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
                if (seat == null)
                {

                    <div style="height: 130px" class="btn btn-success col-md-2">
                        <label class="control-label">@seatNumber </label>
                    </div>
                }
                else if (seat.IsMine && seat != null)
                {
                    <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
                        <label class="control-label">@seatNumber - @seat.Supplier</label><br />
                        <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
                        <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
                    </div>
                }
                else
                {
                    <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
                        <label class="control-label">@seatNumber </label>
                    </div>
                }
            }
            &nbsp;

            @{
                seatNumber = 20;
                seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
                if (seat == null)
                {

                    <div style="height: 130px" class="btn btn-success col-md-2">
                        <label class="control-label">@seatNumber </label>
                    </div>
                }
                else if (seat.IsMine && seat != null)
                {
                    <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
                        <label class="control-label">@seatNumber - @seat.Supplier</label><br />
                        <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
                        <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
                    </div>
                }
                else
                {
                    <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
                        <label class="control-label">@seatNumber </label>
                    </div>
                }
            }
            &nbsp;
      </div>
      <!--#endregion -->
      <!--#region ----21 22 -------  -->
      <div class="row pt-1  ">
         <div class="col-md-2 col-sm-2"></div> &nbsp;
            <div class="col-md-2 col-sm-2"></div>&nbsp;
            <div class="col-md-1 col-sm-1"></div>

        @{
          seatNumber = 21;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
             <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
        @{
          seatNumber = 22;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
             <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        </div>

        <div class="row pt-1">
            <div class="col-md-2 col-sm-2"></div> &nbsp;
            <div class="col-md-2 col-sm-2"></div>&nbsp;
            <div class="col-md-1 col-sm-1"></div>
            @{
          seatNumber = 23;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br />
              <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
          <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
        @{
          seatNumber = 24;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);
          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
             <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
            <div class=" col-md-2 col-sm-2 "></div>
      </div>
      <!--#endregion -->
      <!--#region ----25 26 27 28-------  -->
     
      <div class="row pt-1">
        @{
          seatNumber = 25;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        @{
          seatNumber = 26;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        <div class="col-md-1 col-sm-1"></div>
        @{
          seatNumber = 27;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
        @{
          seatNumber = 28;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
      </div>
      <!--#endregion -->
      <!--#region ----33 34 35 36-------  -->
      <div class="row pt-1">
        @{
          seatNumber = 29;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        @{
          seatNumber = 30;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        <div class="col-md-1 col-sm-1"></div>
        @{
          seatNumber = 31;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
        @{
          seatNumber = 32;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
      </div>
      <!--#endregion -->
      <!--#region ----37 38 39 40-------  -->
      <div class="row pt-1">
        @{
          seatNumber = 33;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        @{
          seatNumber = 34;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        <div class="col-md-1 col-sm-1"></div>
        @{
          seatNumber = 35;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
        @{
          seatNumber = 36;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
              </div>
          }
        }
        &nbsp;
      </div>
      <!--#endregion -->
      <!--#region ----41 42 43 44-------  -->
      <div class="row pt-1">
        @{
          seatNumber = 37;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
      }
        &nbsp;

        @{
          seatNumber = 38;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        <div class="col-md-1 col-sm-1"></div>
        @{
          seatNumber = 39;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
  <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
        @{
          seatNumber = 40;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
      </div>
      <!--#endregion -->
        <!--#region ----41 42 43 44-------  -->
        <div class="row pt-1">
            @{
                seatNumber = 41;
                seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

                if (seat == null)
                {

                    <div style="height: 130px" class="btn btn-success col-md-2">
                        <label class="control-label">@seatNumber </label>
                    </div>
                }
                else if (seat.IsMine && seat != null)
                {
                    <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
                        <label class="control-label">@seatNumber - @seat.Supplier</label><br />
                        <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
                        <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
                    </div>
                }
                else
                {
                    <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
                        <label class="control-label">@seatNumber </label>
                    </div>
                }
            }
            &nbsp;

            @{
                seatNumber = 42;
                seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

                if (seat == null)
                {

                    <div style="height: 130px" class="btn btn-success col-md-2">
                        <label class="control-label">@seatNumber </label>
                    </div>
                }
                else if (seat.IsMine && seat != null)
                {
                    <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
                        <label class="control-label">@seatNumber - @seat.Supplier</label><br />
                        <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
                        <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
                    </div>
                }
                else
                {
                    <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
                        <label class="control-label">@seatNumber </label>
                    </div>
                }
            }
            &nbsp;

            <div class="col-md-1 col-sm-1"></div>
            @{
                seatNumber = 43;
                seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

                if (seat == null)
                {

                    <div style="height: 130px" class="btn btn-success col-md-2">
                        <label class="control-label">@seatNumber </label>
                    </div>
                }
                else if (seat.IsMine && seat != null)
                {
                    <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
                        <label class="control-label">@seatNumber - @seat.Supplier</label><br />
                        <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
                        <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
                    </div>
                }
                else
                {
                    <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
                        <label class="control-label">@seatNumber </label>
                    </div>
                }
            }
            &nbsp;
            @{
                seatNumber = 44;
                seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

                if (seat == null)
                {

                    <div style="height: 130px" class="btn btn-success col-md-2">
                        <label class="control-label">@seatNumber </label>
                    </div>
                }
                else if (seat.IsMine && seat != null)
                {
                    <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
                        <label class="control-label">@seatNumber - @seat.Supplier</label><br />
                        <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
                        <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
                    </div>
                }
                else
                {
                    <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
                        <label class="control-label">@seatNumber </label>
                    </div>
                }
            }
            &nbsp;
        </div>
        <!--#endregion -->
      <!--#region ----45 46 47 48 49-------  -->
      <div class="row pt-1">
        @{
          seatNumber = 45;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;

        @{
          seatNumber = 46;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;


        @{
          seatNumber = 47;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
        @{
          seatNumber = 48;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
        @{
          seatNumber = 49;
          seat = Model.reservedTickets.FirstOrDefault(x => x.SeatId == seatNumber);

          if (  seat == null)
          {

            <div style="height: 130px" class="btn btn-success col-md-2">
              <label class="control-label">@seatNumber </label>
            </div>
          }
          else if (seat.IsMine && seat != null)
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber - @seat.Supplier</label><br />
              <label class="control-label">@seat.FromBranch --> @seat.ToBranch</label><br />
              <label class="control-label"> @(seat.Customer)</label><br /> <label class="control-label">@seat.Phone</label>
            </div>
          }
          else
          {
            <div style="height: 130px" class="@(seat.IsPaid ? "btn btn-danger col-md-2  cancelTicket " : "btn btn-info col-md-2  cancelTicket")">
              <label class="control-label">@seatNumber </label>
            </div>
          }
        }
        &nbsp;
      </div>
      <!--#endregion -->

    </div>
  </div>


 

@section Scripts
        {
  @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
  <script type="text/javascript"> 
    $(document).ready(function() {

    });
  </script>
}